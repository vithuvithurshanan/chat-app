# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          # try multiple common secret names as a fallback so the workflow is more robust
          # repository admins can set any of these secrets (preferred: FIREBASE_SERVICE_ACCOUNT)
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WOLF_CHAT_2C2B9 || secrets.FIREBASE_SERVICE_ACCOUNT || secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
          channelId: live
          projectId: wolf-chat-2c2b9
